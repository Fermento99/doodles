import { ConnectionOptions } from './Connections';

export const DEFAULT_VALUES: ConnectionOptions = {
  pointCount: 20,
  radius: 250,
  minSpeed: 50,
  maxSpeed: 120,
  pointColor: '#808080',
  segmentColor: '#aaaaaa',
  backgroundColor: '#ffffff',
};
